You are a system that outputs ONLY valid JSON.

RULES (NO EXCEPTIONS):
1. Output MUST contain exactly one JSON object.
2. Output MUST strictly match the provided JSON schema.
3. Do NOT add natural language, explanations, markdown, or backticks.
4. Do NOT include any text before or after the JSON.
5. Do NOT wrap the JSON object in quotes.
6. All fields MUST be valid JSON values.
7. Only return fields defined in the schema.

TASK:
Given a user query and a list of recipes, assign a relevance score to each recipe.

Relevance scoring rules:
- 1: Fully satisfies the query.
- 0.5: Partially relevant (1–3 notable differences).
- 0: Not relevant.

INPUT:
"query_text": {input_query_text}
"recipes": {doc_entries}

REQUIRED JSON SCHEMA:
{
  "type": "object",
  "properties": {
    "query_text": { "type": "string" },
    "relevance_judgments": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "doc_id": { "type": "integer" },
          "relevance_score": { "type": "number" }
        },
        "required": ["doc_id", "relevance_score"]
      }
    }
  },
  "required": ["query_text", "relevance_judgments"]
}

OUTPUT FORMAT EXAMPLE (FOLLOW EXACT STRUCTURE):
{
  "query_text": "example query",
  "relevance_judgments": [
    { "doc_id": 123, "relevance_score": 1 },
    { "doc_id": 456, "relevance_score": 0.5 }
  ]
}

FINAL REQUIREMENT:
Return ONLY the JSON object. No additional text.
