from typing import Optional, Dict, Any, List
from pydantic import BaseModel, Field


class Request(BaseModel):
    # In here we can define various fields for the Request model, for now it's empty
    pass

class Response(BaseModel):
    # In here we can define various fields for the Response model, for now it's empty
    pass